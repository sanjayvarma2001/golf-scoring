# The assignment PRD is marked "issued for selection process only" - keep it
# out of git so it never ends up in a public repo's history. Delete this
# line if you've confirmed it's fine to include.
Digital Heroes PRD (Level 1).pdf

.vscode/
